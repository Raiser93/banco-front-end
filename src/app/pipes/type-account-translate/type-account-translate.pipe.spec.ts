import { TypeAccountTranslatePipe } from './type-account-translate.pipe';

describe('TypeAccountTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAccountTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
